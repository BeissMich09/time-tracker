import React from "react";
import styles from "./TimeTracker.module.css";

const TimeTracker = () => {
    return (
        <div className={styles.time_tracker}>TimeTracker</div>
    )
}

export default TimeTracker;